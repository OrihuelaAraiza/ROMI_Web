# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=romi
DB_PASS=romi
DB_NAME=romi
DATABASE_URL=postgresql://romi:romi@localhost:5432/romi

# Auth
JWT_SECRET=change-me-in-production

# CORS – comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3002

# App
PORT=3001
NODE_ENV=development

# Contact email (SMTP)
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true        
SMTP_USER=resend
SMTP_PASS=resend_pass_here
CONTACT_TO_EMAIL=contacto@romiai.com.mx
CONTACT_FROM_EMAIL=no-reply@romiai.com.mx

